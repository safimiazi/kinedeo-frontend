# Development Dockerfile — hot reload with Next.js dev server
FROM node:24-alpine
WORKDIR /app

ENV NODE_ENV=development

COPY package*.json ./
RUN npm ci

# Source code is mounted as a volume — no COPY needed
EXPOSE 3000

CMD ["npm", "run", "dev"]
